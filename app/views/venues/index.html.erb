<h1>Venue Index</h1>

<h4>Search by venue name:</h4>
<%= form_tag(venues_path, method: :get) do %>
<%= text_field_tag(:term, params[:term]) %>
<%= submit_tag 'Search', name: nil %>

<% end %>

<% @venues.each do |venue| %>
<p> <%= link_to venue.name, venue_path(venue.id) %> </p>
<% end %>
